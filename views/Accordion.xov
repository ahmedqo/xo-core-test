<div class="{{classes.colContainer}}">
    {*set themes = ["none", "water", "flame", "woods", "earth", "metal"]*}
    {*each themes as theme*}
        <xo-accordion theme="{{theme}}" key="{{$i}}" header="Title name">
            {{>>randemize()}}
            {{>randemize()}}
        </xo-accordion>
    {*/each*}
</div>